var mongoose = require('mongoose');
var express = require('express');
var storefront = require(__dirname + '/../models/storefront');

var shopRouter = module.exports = express.router();

shopRouter.get('/shop', function(req, res) {
  
});
